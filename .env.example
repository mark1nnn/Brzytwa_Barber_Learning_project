# Astro build-time values. Copy this file to .env for local builds only.
# Replace the reserved URL before any preview or production deployment.
PUBLIC_SITE_URL=https://example.invalid

# Public widget key. The booking form stays disabled while this placeholder is present.
PUBLIC_TURNSTILE_SITE_KEY=replace-with-turnstile-site-key
